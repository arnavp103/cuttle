[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "websockets>=14.1",
    "fastapi",
    "uvicorn[standard]",
]

[project.group.dev.dependencies]
httpx = "*"
